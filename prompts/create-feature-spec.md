# Create Feature Spec Prompt

Create or refine a feature spec in `docs/03-features/`. Include problem, goal, users, MVP relevance, non-goals, user stories, acceptance criteria, affected files, dependencies, and open questions.
