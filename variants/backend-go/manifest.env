# Variant manifest - sourced by bin/devblueprint. Shell key=value pairs.
VARIANT_TITLE="Backend service (Go)"
QUALITY_GATE="sh scripts/check-env.sh && test -z \"\$(gofumpt -l .)\" && go vet ./... && golangci-lint run && go build ./... && go test -race ./..."
WT_CMD="./scripts/wt.sh"
COPY_LANGUAGE_NOTE="User-facing strings (API error messages, log labels, CLI help) come from a dedicated messages package or config, not scattered string literals through the handlers."
SRC_DIRS="cmd internal pkg"
