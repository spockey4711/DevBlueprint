# Variant manifest - sourced by bin/devblueprint. Shell key=value pairs.
VARIANT_TITLE="Data science (Python + uv)"
QUALITY_GATE="sh scripts/check-env.sh && ruff check . && ruff format --check . && nbqa ruff notebooks && mypy src && pytest"
WT_CMD="./scripts/wt.sh"
COPY_LANGUAGE_NOTE="Plots, reports and any user-facing labels read from a config/constants module, not hard-coded through the analysis code."
SRC_DIRS="src notebooks tests data/raw data/processed data/external"
