# Variant manifest - sourced by bin/devblueprint. Shell key=value pairs.
VARIANT_TITLE="Flutter app (Dart)"
QUALITY_GATE="dart format --output=none --set-exit-if-changed . && flutter analyze && flutter test"
WT_CMD="./scripts/wt.sh"
COPY_LANGUAGE_NOTE="User-facing strings live in ARB files under lib/l10n (AppLocalizations), never hard-coded literals in widgets."
SRC_DIRS="lib lib/src test integration_test"
