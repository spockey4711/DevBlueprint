# Variant manifest - sourced by bin/devblueprint. Shell key=value pairs.
VARIANT_TITLE="Rust (cargo)"
QUALITY_GATE="sh scripts/check-env.sh && cargo fmt --check && cargo clippy --all-targets --all-features -- -D warnings && cargo test"
WT_CMD="./scripts/wt.sh"
COPY_LANGUAGE_NOTE="User-facing strings live in a dedicated module or a localization crate (e.g. fluent), never scattered as literals through the code."
SRC_DIRS="src tests"
