# Variant manifest - sourced by bin/devblueprint. Shell key=value pairs.
VARIANT_TITLE="Backend / web app (Elixir / Phoenix)"
QUALITY_GATE="mix format --check-formatted && mix credo --strict && mix dialyzer && mix test"
WT_CMD="./scripts/wt.sh"
COPY_LANGUAGE_NOTE="User-facing strings (validation messages, flash notices, template copy) come from Phoenix's gettext (priv/gettext/*.po resolved through the gettext/1 macro and the ~p-style helpers), not scattered string literals through the controllers and templates."
SRC_DIRS="lib test"
