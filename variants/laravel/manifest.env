# Variant manifest - sourced by bin/devblueprint. Shell key=value pairs.
VARIANT_TITLE="Backend / web app (PHP / Laravel)"
QUALITY_GATE="vendor/bin/pint --test && vendor/bin/phpstan analyse && vendor/bin/pest"
WT_CMD="./scripts/wt.sh"
COPY_LANGUAGE_NOTE="User-facing strings (validation messages, notifications, UI copy) come from Laravel's localization files (lang/ resolved through __() / trans()), not scattered string literals through the controllers and views."
SRC_DIRS="app tests"
