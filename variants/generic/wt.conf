# wt.conf - worktree config for a language-agnostic project. Sourced by scripts/wt.sh.
MAIN_BRANCH="master"
BASE_BRANCH="develop"
ALLOWED_TYPES="feat fix docs style refactor perf test build ci chore revert"

# No dependency install by default. Override this hook if your project needs one,
# e.g.  ( cd "$1" && make setup )
wt_post_create() { :; }
