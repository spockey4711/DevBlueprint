# Variant manifest - sourced by bin/devblueprint. Shell key=value pairs.
VARIANT_TITLE="Web app (Next.js + pnpm)"
QUALITY_GATE="sh scripts/check-env.sh && pnpm lint && pnpm typecheck && pnpm test && pnpm build"
WT_CMD="pnpm wt"
COPY_LANGUAGE_NOTE="Localize user-facing copy in a dedicated content layer, never as scattered string literals."
# Source directories to scaffold with a .gitkeep so the layout exists from day one.
SRC_DIRS="src/app src/components src/lib src/features src/styles tests/unit tests/e2e"
