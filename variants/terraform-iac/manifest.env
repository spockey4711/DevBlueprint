# Variant manifest - sourced by bin/devblueprint. Shell key=value pairs.
VARIANT_TITLE="Infrastructure as Code (Terraform)"
QUALITY_GATE="sh scripts/check-env.sh && terraform fmt -check -recursive && terraform validate && tflint --recursive && terraform test"
WT_CMD="./scripts/wt.sh"
COPY_LANGUAGE_NOTE="Human-facing strings (resource tags, variable/output description fields, module docs) come from variables and locals with clear descriptions, not hard-coded literals scattered across resources."
SRC_DIRS="modules tests"
