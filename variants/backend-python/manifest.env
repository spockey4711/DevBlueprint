# Variant manifest - sourced by bin/devblueprint. Shell key=value pairs.
VARIANT_TITLE="Backend / API (Python + uv)"
QUALITY_GATE="ruff check . && ruff format --check . && mypy . && pytest"
WT_CMD="./scripts/wt.sh"
COPY_LANGUAGE_NOTE="User-facing strings and error messages live in a dedicated module, not scattered through handlers."
SRC_DIRS="app/routes app/services app/models app/schemas app/core tests/unit tests/integration"
