# Variant manifest - sourced by bin/devblueprint. Shell key=value pairs.
VARIANT_TITLE="iOS app (Swift + Xcode)"
QUALITY_GATE="swiftformat --lint . && swiftlint --strict && swift build && swift test"
WT_CMD="./scripts/wt.sh"
COPY_LANGUAGE_NOTE="User-facing strings live in String Catalogs (.xcstrings)/Localizable, never as literals in views."
SRC_DIRS="Sources/App Sources/Core Sources/Features Sources/DesignSystem Sources/Shared Tests/UnitTests Tests/UITests"
