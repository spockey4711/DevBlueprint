# Variant manifest - sourced by bin/devblueprint. Shell key=value pairs.
VARIANT_TITLE="Web app (Ruby on Rails)"
QUALITY_GATE="bundle exec rubocop && bundle exec brakeman -q --no-pager && bundle exec rails test"
WT_CMD="./scripts/wt.sh"
COPY_LANGUAGE_NOTE="User-facing strings (flash messages, view copy, mailer subjects) come from I18n locale files (config/locales/*.yml) resolved through I18n.t / the t helper, not string literals scattered through controllers and views."
SRC_DIRS="app lib test"
