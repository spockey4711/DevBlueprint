# Variant manifest - sourced by bin/devblueprint. Shell key=value pairs.
VARIANT_TITLE="Backend service (C# / .NET)"
QUALITY_GATE="dotnet format --verify-no-changes && dotnet build --configuration Release && dotnet test"
WT_CMD="./scripts/wt.sh"
COPY_LANGUAGE_NOTE="User-facing strings (API error messages, log labels) come from a resource file (.resx) resolved through IStringLocalizer, not scattered string literals through the controllers and services."
SRC_DIRS="src tests"
