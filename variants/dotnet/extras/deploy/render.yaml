# Render Blueprint (skeleton) - https://render.com/docs/blueprint-spec
# Commit this, connect the repo in Render, and it provisions the service from the
# repo's Dockerfile. Set real secrets in the Render dashboard (or keep `sync: false`
# below so Render prompts for them) - never commit them here. See docs/ops/deployment.md.
services:
  - type: web
    name: <your-app-name>
    runtime: docker            # or the native `dotnet` runtime if you prefer no Dockerfile
    dockerfilePath: ./Dockerfile
    healthCheckPath: <health-check-path>   # e.g. /healthz
    autoDeploy: true
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: ASPNETCORE_URLS
        value: http://+:8080
      - key: ConnectionStrings__Default   # secret/managed - set it in the dashboard, not here
        sync: false
