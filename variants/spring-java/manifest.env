# Variant manifest - sourced by bin/devblueprint. Shell key=value pairs.
VARIANT_TITLE="Backend service (Java + Spring Boot)"
QUALITY_GATE="sh scripts/check-env.sh && ./gradlew spotlessCheck checkstyleMain checkstyleTest test"
WT_CMD="./scripts/wt.sh"
COPY_LANGUAGE_NOTE="User-facing strings (API error messages, log labels) come from a dedicated messages source (MessageSource / messages.properties), not scattered string literals through the controllers and services."
SRC_DIRS="src/main/java src/main/resources src/test/java src/test/resources"
